export class Product {
  constructor(
    public id: number,
    public code: string,
    public name: string,
    public tag: string
  ) {}
}

export class Product2 {
  constructor(
    public code: string,
    public name: string,
    public tag: string
  ) {}
}
